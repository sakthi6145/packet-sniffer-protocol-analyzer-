# packet-sniffer-protocol-analyzer-
packet sniffer that captures live network traffic and displays source IP, destination IP, protocol, and packet length for real-time packet analysis. Captures live network packets Displays Source IP Address Displays Destination IP Address Identifies network protocols (TCP, UDP, ICMP), Displays packet length packet analysis tool Technologies 
